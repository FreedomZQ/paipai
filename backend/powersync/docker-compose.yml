version: "3.9"
services:
  powersync:
    image: journeyapps/powersync-service:latest
    env_file:
      - .env
    ports:
      - "${POWERSYNC_PORT:-8086}:8080"
    volumes:
      - ./common:/config/common:ro
      - ./apps:/config/apps:ro
    restart: unless-stopped
